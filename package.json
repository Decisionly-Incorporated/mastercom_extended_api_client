{
  "name": "mastercom_extended_api_client",
  "version": "1.0.0",
  "description": "Generated Mastercom Extended API client",
  "main": "api.js",
  "scripts": {
    "postinstall": "npm run build",
    "build": "tsc",
    "clean": "tsc --clean",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.13.4",
    "@types/node": "^20.14.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "request": "^2.88.2"
  }
}
