/**
 * MasterCom
 * MasterCom
 *
 * The version of the OpenAPI document: v6
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ChargebackMarkProcessedRequestStructure model module.
 * @module model/ChargebackMarkProcessedRequestStructure
 * @version v6
 */
class ChargebackMarkProcessedRequestStructure {
    /**
     * Constructs a new <code>ChargebackMarkProcessedRequestStructure</code>.
     * @alias module:model/ChargebackMarkProcessedRequestStructure
     * @param claimId {String} Claim Id.   Length: 1-19   Valid Values/Format: Numeric
     * @param chargebackId {String} Chargeback Id.   Length: 1-19   Valid Values/Format: Numeric
     */
    constructor(claimId, chargebackId) { 
        
        ChargebackMarkProcessedRequestStructure.initialize(this, claimId, chargebackId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, claimId, chargebackId) { 
        obj['claimId'] = claimId;
        obj['chargebackId'] = chargebackId;
    }

    /**
     * Constructs a <code>ChargebackMarkProcessedRequestStructure</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ChargebackMarkProcessedRequestStructure} obj Optional instance to populate.
     * @return {module:model/ChargebackMarkProcessedRequestStructure} The populated <code>ChargebackMarkProcessedRequestStructure</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ChargebackMarkProcessedRequestStructure();

            if (data.hasOwnProperty('claimId')) {
                obj['claimId'] = ApiClient.convertToType(data['claimId'], 'String');
            }
            if (data.hasOwnProperty('chargebackId')) {
                obj['chargebackId'] = ApiClient.convertToType(data['chargebackId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ChargebackMarkProcessedRequestStructure</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ChargebackMarkProcessedRequestStructure</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ChargebackMarkProcessedRequestStructure.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['claimId'] && !(typeof data['claimId'] === 'string' || data['claimId'] instanceof String)) {
            throw new Error("Expected the field `claimId` to be a primitive type in the JSON string but got " + data['claimId']);
        }
        // ensure the json data is a string
        if (data['chargebackId'] && !(typeof data['chargebackId'] === 'string' || data['chargebackId'] instanceof String)) {
            throw new Error("Expected the field `chargebackId` to be a primitive type in the JSON string but got " + data['chargebackId']);
        }

        return true;
    }


}

ChargebackMarkProcessedRequestStructure.RequiredProperties = ["claimId", "chargebackId"];

/**
 * Claim Id.   Length: 1-19   Valid Values/Format: Numeric
 * @member {String} claimId
 */
ChargebackMarkProcessedRequestStructure.prototype['claimId'] = undefined;

/**
 * Chargeback Id.   Length: 1-19   Valid Values/Format: Numeric
 * @member {String} chargebackId
 */
ChargebackMarkProcessedRequestStructure.prototype['chargebackId'] = undefined;






export default ChargebackMarkProcessedRequestStructure;

